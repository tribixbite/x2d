diff --git a/src/CMakeLists.txt b/src/CMakeLists.txt
index cd37547..fc253dc 100644
--- a/src/CMakeLists.txt
+++ b/src/CMakeLists.txt
@@ -36,22 +36,18 @@ if (SLIC3R_GUI)
         message(STATUS "WXWIN environment set to: $ENV{WXWIN}")
     elseif(UNIX)
         set(wxWidgets_USE_UNICODE ON)
-        if(SLIC3R_STATIC)
-            set(wxWidgets_USE_STATIC ON)
-        else()
-            set(wxWidgets_USE_STATIC OFF)
-        endif()
+        set(wxWidgets_USE_STATIC OFF)
     endif()
 
     if (CMAKE_SYSTEM_NAME STREQUAL "Linux")
         set (wxWidgets_CONFIG_OPTIONS "--toolkit=gtk${SLIC3R_GTK}")
         if (SLIC3R_WX_STABLE)
-            find_package(wxWidgets 3.0 REQUIRED COMPONENTS base core adv html gl aui net media webview)
+            find_package(wxWidgets 3.0 REQUIRED COMPONENTS base core adv html gl aui net webview)
         else ()
-            find_package(wxWidgets 3.1 REQUIRED COMPONENTS base core adv html gl aui net media webview)
+            find_package(wxWidgets 3.1 REQUIRED COMPONENTS base core adv html gl aui net webview)
         endif ()
     else ()
-        find_package(wxWidgets 3.1 REQUIRED COMPONENTS html adv gl core base webview aui net media)
+        find_package(wxWidgets 3.1 REQUIRED COMPONENTS html adv gl core base webview aui net)
     endif ()
 
     if(UNIX)
@@ -139,7 +135,7 @@ elseif (MSVC)
     set(CMAKE_EXE_LINKER_FLAGS "${CMAKE_EXE_LINKER_FLAGS} /MANIFEST:NO")
 else ()
     # Boost on Raspberry-Pi does not link to pthreads explicitly.
-    target_link_libraries(BambuStudio ${CMAKE_DL_LIBS} -lstdc++ Threads::Threads pangoft2-1.0)
+    target_link_libraries(BambuStudio ${CMAKE_DL_LIBS} -lstdc++ Threads::Threads pangoft2-1.0 X11)
 endif ()
 
 # Add the Slic3r GUI library, libcurl, OpenGL and GLU libraries.
