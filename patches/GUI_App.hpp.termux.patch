diff --git a/src/slic3r/GUI/GUI_App.hpp b/src/slic3r/GUI/GUI_App.hpp
index f80a20e..0eb995c 100644
--- a/src/slic3r/GUI/GUI_App.hpp
+++ b/src/slic3r/GUI/GUI_App.hpp
@@ -588,6 +588,12 @@ public:
 
     void            report_consent_common(bool agree, std::string scene, std::string formID);
     virtual bool OnExceptionInMainLoop();
+    // Termux source patch (item #24): swallow noisy wxWidgets sizer
+    // assertions (CheckExpectedParentIs) that BambuStudio's slice path
+    // triggers but that are harmless. Logs once instead of popping a
+    // modal asking for Continue/Stop.
+    void OnAssertFailure(const wxChar *file, int line, const wxChar *func,
+                         const wxChar *cond, const wxChar *msg) override;
     // Calls wxLaunchDefaultBrowser if user confirms in dialog.
     bool            open_browser_with_warning_dialog(const wxString& url, int flags = 0);
 #ifdef __APPLE__
